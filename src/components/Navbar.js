import React from "react";

import './NavBar.scss'

const NavBar = () => {
  return (
    <nav className="NavBar">
      <span className="heading">Where in the World?</span>

      <span className="dm-btn">Dark Mode</span>
    </nav>
  );
};

export default NavBar;
